## Local folder rename to model-archival

- Renamed repository subfolder `model-archival/` to `model-archival/`.
- Updated active operational references from `model-archival/...` to `model-archival/...` across:
  - root docs (`README.md`, `docs/*`)
  - gdrive configs/docs (`gdrive-archival/*`)
  - workspace rules in `.cursor/rules/*`
  - in-folder docs/scripts/config comments under `model-archival/`
- Intentionally left historical chat/changelog references unchanged where they describe past state.
- Verified no active `model-archival/` path references remain outside historical files and shell setup `.local/bin` strings.
