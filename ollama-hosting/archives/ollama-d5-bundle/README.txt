Local staging for scripts/archive-ollama-github-to-d5.sh before rsync to the archival VM
(/mnt/models/d5/ollama): pinned/<tag>/ and latest/ (or symlink). Default staging dir: **root/** (legacy **tree/** may be deleted). Not committed.
