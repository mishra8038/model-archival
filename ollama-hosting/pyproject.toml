[project]
name = "ollama-hosting-tools"
version = "0.1.0"
description = "Scripts and docs for Supermicro Ollama hosting + archival VM sync (model-archival monorepo)"
requires-python = ">=3.11"
readme = "README.md"
dependencies = [
    "httpx>=0.27",
    "pyyaml>=6.0",
]

[tool.uv]
package = false
