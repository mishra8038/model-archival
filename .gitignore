# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets — never commit tokens or credentials
*.token
.hf_token
model-archival/deploy/sethfToken.sh
**/HF_TOKEN
.env
*.env

# Runtime outputs (live on storage drives, not in repo)
run-report-*.md
model-archival/deploy/verify-environment-report-*.md
model-archival/deploy/run-report-*.md
model-archival/verification/verification-reports/
model-archival/chat/
.chat/

# fingerprints sub-project
fingerprints/.venv/
fingerprints/data/
fingerprints/output/

# GDrive backup — contains OAuth token
gdrive-archival/rclone.conf

# Large binaries / installers (exceed GitHub 100 MB limit)
model-archival/google-cloud-cli-linux-x86_64.tar.gz

# code-archival sub-project
code-archival/.secrets
code-archival/*.token
code-archival/*.key
code-archival/output/
code-archival/*.part
code-archival/*.tmp

# Large artifacts
agent-tools/graphcore-sdk/*.tar.gz
agent-tools/graphcore-repo-tarballs/*.tar.gz
