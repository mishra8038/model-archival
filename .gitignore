# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets — never commit tokens or credentials
*.token
.hf_token
local/deploy/sethfToken.sh
**/HF_TOKEN
.env
*.env

# Runtime outputs (live on storage drives, not in repo)
run-report-*.md
local/deploy/verify-environment-report-*.md
local/deploy/run-report-*.md
local/verification/verification-reports/
local/chat/
.chat/

# fingerprints sub-project
fingerprints/.venv/
fingerprints/data/
fingerprints/output/

# GDrive backup — contains OAuth token
gdrive-archival/rclone.conf

# Large binaries / installers (exceed GitHub 100 MB limit)
local/google-cloud-cli-linux-x86_64.tar.gz

# code-archival sub-project
code-archival/.secrets
code-archival/*.token
code-archival/*.key
code-archival/output/
code-archival/*.part
code-archival/*.tmp
